server:
  port: 8189
  servlet:
    context-path: /app
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:49153/skillbox
    username: postgres
    password: postgrespw
  jpa:
    show-sql: true